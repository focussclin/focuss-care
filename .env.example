# Focuss Care — variáveis de ambiente
# Copie para .env.local e preencha. NUNCA commite .env.local (já está no .gitignore).

# ---------------------------------------------------------------------------
# Supabase — público (vai para o navegador, pode ser exposto)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# ---------------------------------------------------------------------------
# Supabase — SEGREDO (somente servidor, ignora RLS, nunca prefixar NEXT_PUBLIC_)
# ---------------------------------------------------------------------------
SUPABASE_SECRET_KEY=

# ---------------------------------------------------------------------------
# Supabase CLI / Management API (somente máquina de dev e CI)
# ---------------------------------------------------------------------------
SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=

# ---------------------------------------------------------------------------
# IA (fase posterior — somente servidor)
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=
